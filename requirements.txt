# ==========================================
# Web Server & Infrastructure
# ==========================================
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0
anyio==4.9.0
python-dotenv==1.0.1
psutil==6.1.1
slowapi>=0.1.9
limits>=3.6.0

# ==========================================
# Data Validation & Requests
# ==========================================
pydantic==2.9.2
pydantic-settings==2.2.1
python-multipart==0.0.12
requests==2.32.3
pandas==2.2.3

# ==========================================
# Core Machine Learning & Deep Learning (API Runtime)
# ==========================================
tensorflow-cpu==2.17.1
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu

# Library Pendukung Inferensi & Image Processing
opencv-python==4.10.0.84
Pillow==10.4.0
ultralytics==8.3.149
scikit-learn==1.5.2
scipy==1.13.1
numpy==1.26.4
joblib==1.4.2
scikit-image==0.23.2
transformers==4.40.2
diffusers==0.28.0
huggingface-hub==0.24.7
accelerate>=0.26.0
faiss-cpu==1.8.0

# FIX 1: matplotlib 3.8.2 has a circular import bug on Python 3.12.
#         Upgrade to 3.9.x which resolves the '_version' ImportError.
matplotlib==3.9.4

# FIX 2: mmpycocotools requires numpy at build time but pip doesn't install
#         it first. Two options (pick one):
#
# Option A (recommended — Windows prebuilt wheel, no compiler needed):
pycocotools==2.0.8
#
# Option B (if you still need mmpycocotools specifically):
# Install numpy first manually, then install mmpycocotools:
#   pip install numpy==1.26.4
#   pip install mmpycocotools==12.0.3
# Do NOT include mmpycocotools here if using Option A above.

# Notes:
# - tensorflow-cpu 2.17.1 adds official Python 3.12 support (2.17.0 was 3.11 only).
# - matplotlib 3.9.x fixes the '_version' circular import present in 3.8.2 on Py3.12.
# - mmpycocotools replaced with pycocotools 2.0.8 which ships prebuilt Windows wheels.
# - torch/torchvision pinned to +cpu suffix for clarity with the extra-index-url above.
# - On Windows, faiss-cpu 1.8.0 may still fail for some builds.
#   If so, install via conda: conda install -c conda-forge faiss-cpu

# ==========================================
# Miscellaneous & Security
# ==========================================
python-jose[cryptography]==3.3.0
fastapi-csrf-protect==0.3.3
boto3==1.34.150

# ==========================================
# Testing (required by FastAPI TestClient)
# ==========================================
httpx==0.27.0